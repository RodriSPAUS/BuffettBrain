# Agent entry point

The operating instructions for this repository live in **`AGENT.md`** in this
same directory. Read it in full before doing anything else — it defines the
Ingest / Query / Lint operations and the citation-integrity rules.

Page structure and frontmatter conventions live in `wiki/SCHEMA.md`.

This file is a pointer, not a copy: do not duplicate rules here.
