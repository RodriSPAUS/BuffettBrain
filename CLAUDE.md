# Claude Code entry point

The operating instructions for this repository live in `AGENTS.md`, which is
provider-agnostic and self-contained. This file exists only so Claude Code
auto-loads them; the `@` below is an import, not a suggestion to go and read it.

@AGENTS.md
